"""Generic rubric-scored text generation environment for SkillOpt.

This environment intentionally contains no Litigo-specific data. Private task
sets and skills can live in a separate private repository and be supplied via
``env.split_dir`` and ``env.skill_init``.
"""
